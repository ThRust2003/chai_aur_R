
function App() {
  return (
   <div>
    <h1>hell</h1>
    </div>
  );
}

 export default App;
// 🟢 npm
// 📦 Used to install packages

// Can install:

// Locally (for your project)

// Globally (for your whole system)

// Adds packages to:

// node_modules/

// package.json (if saved)

// 🔁 Stays on your system (until you uninstall)

// 🧱 Used to add dependencies you’ll use in your app

// 📌 Think of it like:
// “Bring it in and keep it.”

// 🔵 npx
// ⚡ Used to run packages immediately

// Doesn’t install permanently

// Fetches package, runs it, deletes it (or caches temporarily)

// Perfect for:

// Scaffolding (create-react-app, vite, express-generator)

// One-time tools (prettier, eslint, http-server)

// 🧼 Keeps your system clean

// 📌 Think of it like:
// “Use it, lose it.”
//EXCEPTION NPM CREATRE, NPM EXEC,NPM INIT